Language Strictness: Write 100% pure JavaScript (.js). NEVER write .ts files. NEVER use TypeScript syntax (interfaces, type aliases) in executable code. ALL typing must be done via JSDoc comments.

Read CONTEXT.md when you need to make any big changes to the repo, for anything bigger than a quick fix.

Read docs/GAME_DESIGN.md when analyzing game rules or updating the AI to make sure you understand the game. The players play on their own board the theater is NOT shared, theres no advantage to playing a card first.
